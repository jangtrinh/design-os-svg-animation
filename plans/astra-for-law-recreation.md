# Astra for Law video recreation

Status: HyperFrames Stage 2 integrated; the revised firm-tile motion and partner stand-ins are exported locally for review, with owner visual acceptance still open. The owner authorized publishing this work in progress to the repository and GitHub Pages on 2026-09-24; that authorization is not a visual acceptance claim. Source: [OpenAI video](https://www.youtube.com/watch?v=YeeGHCixr7o), published 2026-09-17. Passing engineering gates does not establish visual acceptance. The [scene-by-scene motion audit](astra-law-temporal-audit.md) records transition, micro interaction, and camera observations.

## Outcome and boundary

Build a seekable 16:9 Studio Runner and deterministic 30 fps MP4 that recreate the reference's typography, visual sequence, UI walkthrough, motion rhythm, and final mark. Use this repository's Motion IR and geometry/virtual-clock approach. Keep the source download, its soundtrack, reference frames, and local MP4 under ignored cache. The public browser player is explicitly marked as a study while visual review remains open.

Observed reference: 1920x1080, 30 fps, 77.594 seconds. FFmpeg's 0.18 scene threshold found a black-field transition at 7.57-7.63 seconds but missed the frequent soft reveals. Even at 0.06, cut scoring caught only 5.2, 7.53-7.63, 10.67-10.77, 57.9, and 69.5 s, so frame sampling remains necessary. A 50 Hz waveform, keyframe sheets, and Product Designer audit are local evidence for this revision. Time ranges below are approximate semantic anchors, not exact cut claims.

| Time (s) | Visual beat |
| --- | --- |
| 0-4 | OpenAI for Law; frontier intelligence for professional legal work |
| 4-10 | Blue point into dark spiral; Astra for Law |
| 10-18 | Build your methods; ChatGPT for Your Firm and firm tiles |
| 18-25 | Prompt composer, model label, and typed S-1 request |
| 25-35 | Tool/thinking states, mapping response, attached Word file |
| 35-41 | ChatGPT for Word over coral/orange/lavender field |
| 41-49 | Legal tools heading and orbiting partner tiles |
| 49-53 | Community skills labels |
| 53-60 | Legal-grade trust and controls |
| 60-68 | Zero data retention and automated safeguards |
| 68-75 | Earned trust and firm ambitions |
| 75-77.594 | OpenAI mark |

## Acceptance

1. Scene structure, key text, background, and subject match the sampled reference at representative timepoints; mark visual differences honestly.
2. HyperFrames Stage 2 maps every scene to a real declarative beat. The draft and final player yield the same frame for repeated or out-of-order seeks; timelines are finite and reduced motion has a static fallback.
3. At least one vector element compiles from a schema-valid Motion IR, with transforms sampled from its tracks rather than raw animated path tokens.
4. Interactive Studio Runner includes play/pause, restart, scrub, chapter jumps, speed, fullscreen, and keyboard controls. The clean export frame remains 1920x1080.
5. Run Motion IR validation, project audits, representative frame capture, comparison review, and a complete local MP4 export. Keep reference assets and full video out of Git.

## Fidelity recovery targets

The [Product Designer audit](reports/astra-law-product-designer-audit.md) ranked five gaps: shot/word timing, ChatGPT camera and tool progression, galaxy silhouette, orbit stagger/counter, and accent/outro states. The first cut skipped “Powered by”, “knowledge”, “value”, and “With OpenAI for Law”; showed complete content at 21, 31, 45, and 73 s when the source showed different states; and used the wrong opening accent. Treat these as acceptance blockers. Compare the source and captured draft at matched timestamps with `python3 scripts/compare-astra-law-reference.py /tmp/astra-for-law-reference.mp4` after `node scripts/export-astra-law-video.mjs --proof`.

## Known fidelity limit

The source contains many third-party brand tiles. The partner orbit uses source-verified text where readable and, per the owner's later choice, genuine [SVGL](https://svgl.app/) icons as clearly labeled stand-ins for the other 19 marks. Their brand identities intentionally do not match the reference. The procedural galaxy uses deterministic star positions sampled from one source frame, while the transitions and other UI are rebuilt. Word/Excel marks came from SVGL; the final tie and scales are generated interpretations of the reference, not source cutouts. The reference soundtrack is included only in the ignored local MP4 and is excluded from Git.

## Local handoff

- Studio Runner: `promo/astra-law-promo.html`; serve the repository root with `python3 -m http.server 3033`.
- HyperFrames Stage 2 draft: `promo/astra-law-hyperframes-draft.html`; it uses 23 source-timed beats from `promo/astra-law-hyperframes-beats.mjs` and the local GSAP dependency. It is a monochrome choreography review surface, not the final video layer.
- Motion source: `promo/astra-law.motion.json`; scene beats in `promo/astra-law-timeline.mjs`.
- Icon provenance: 28 local SVGs match `@phosphor-icons/core/assets/{regular,fill}` byte for byte; `openai.svg`, the Microsoft Word/Excel marks, and 19 partner stand-in logos come from SVGL. No partner logo geometry was synthesized. The 19 stand-ins are labeled in `promo/astra-law-partner-orbit.mjs`; their source SVGs are stored under `promo/astra-law-icons/svgl-*.svg`.
- `node scripts/export-astra-law-video.mjs --verify` checks assets, 1920x1080 export geometry, Motion IR cubic path, representative scenes, repeated out-of-order seeks, reduced motion, transport controls, and responsive Runner geometry.
- `node scripts/export-astra-law-video.mjs --proof` writes sampled PNGs to `.cache/astra-for-law/proofs/`.
- `node --test tests/hyperframes.test.mjs` checks the 20-preset vocabulary, mirrored engines, beat mapping, and pure time samplers.
- `node scripts/export-astra-law-video.mjs` exports a silent MP4 to `.cache/astra-for-law/astra-law-recreation.mp4`. The private local review cut with the reference soundtrack uses `--reference-audio /tmp/astra-for-law-reference.mp4` when that local source file is present.
- Export writes a pending MP4 first and replaces the review file only after FFmpeg succeeds, so a Chromium capture failure cannot overwrite a complete cut.

## Verification record

Prior reference comparison at 15, 39, and 63 seconds exposed large visual differences despite some similar coarse bounds. It did not cover missing intermediate shots or camera crops. The comparisons are in ignored `.cache/astra-for-law/`; source frames are not tracked. The owner rejected this first cut as insufficiently similar.

The first cut passed Motion IR validation, TypeScript build, project `audit:all`, 9/9 SVG icon audit, player/Runner verification, and production-only `npm audit --omit=dev --audit-level=high`. Those historical passes did not certify the revised cut. The first full 30 fps H.264/AAC local export decoded with FFmpeg and measured 1920x1080 at 77.600 seconds (frame-rounded from 77.594). The baseline development-only `svgo@4.0.0` advisory remains outside this recreation's scope.

The revised cut restores the missing word/point/closing states, source-timed ChatGPT camera crops and responses, an atomic 27+/36+/40+ sequence, and a staged radial burst to spiral. Product Designer reviewed and edited the visual scenes, including the Word viewer and final app/chart collage. Controller matched 39 draft frames to local reference frames and independently inspected the camera, Word, orbit, and closing boards. Pixel error was used only to locate differences, never as a similarity score. The macro composer now uses CSS layout zoom for crisp arbitrary seeks because Chromium intermittently rasterized transform-scaled text differently on repeated captures; the exporter waits for two animation frames before capture. Ten successive `--verify` stress runs passed after this fix.

The temporal pass replaces the broad scene fades at critical boundaries with a measured black-disk iris, a delayed galaxy exit tunnel, title fragments on a fixed text anchor, a continuous composer camera pullback, a file-card carry into Word, loading dots and staged privacy text, and separately timed closing artifacts. Exact source frames at 7.50, 7.55, 7.60, 7.65, 10.40, 10.60, 37.50, 37.65, 73.80 and 74.80 were used to correct the timing. The final local MP4 is H.264/AAC at 1920×1080, 30 fps, 2,328 frames and 77.600 seconds (frame-rounded from the 77.594 second source). Dedicated browser verification, repeated arbitrary seeks, reduced-motion checks, TypeScript build, `npm run audit:all`, and `git diff --check` all passed after the last visual code edit. FFmpeg decoded 13 exact review frames from the final export. Visual acceptance is still for the owner to decide; identifiable partner marks, some document interior details and the galaxy's photographic texture remain unmatched.

The final read-only Product Designer spot check found no regression in the iris or Word-card fixes. It ranked unverified partner marks at 49.2 s, the soft iris edge at 7.55 s, and the simplified closing collage at 74.8 s as the remaining visible issues.

The next recovery pass followed Astra's five ranked findings against exact source frames. The iris redraws deterministic circular SVG geometry from its Motion IR scale rather than magnifying a rasterized path. The galaxy exit now produces a radial tunnel; photographic bokeh and its particle velocity are still approximate. The response scroll, cursor, file-card carry, and cropped Word reveal now behave as one continuous handoff without nonuniform document scaling. The orbit has 31 source-ordered positions measured at 47.8 s, enters from the right, remains across the skills boundary, and exits clockwise; 25+/27+/29+/40+ counter states were corrected. Its unsourced marks remain explicit placeholders pending an owner choice about local source-frame crops versus the repository's SVGL-only rule. Trust/skills fades, privacy dots and typing, safeguard entry, outro color phases, and final OpenAI mark timing were calibrated against matched frames. Exact logos, Word interior, photo-textured galaxy, and parts of the closing collage remain visible differences; no 100% fidelity claim is made.

Dedicated browser verification now also checks 31 orbit tiles, the settled orbit at 47.8 s, source counter states at 49.8/50.0/50.2 s, Word's undistorted reveal, and full logo presence at 76 s. The final recovery cut passed these checks, Motion IR/HyperFrames unit tests, TypeScript build, `audit:all`, and `git diff --check` after visual code changes. The full local MP4 is re-exported after the final timing edits and must be decoded and visually checked before handoff.

HyperFrames Stage 2 was missing from this cut despite the v2 requirement. The repository's engine exposed only 11 of its documented 20 beat kinds; nine monochrome staging kinds were added and mirrored to the canonical `src/runtime` engine and the two browser copies. The Astra draft maps all 23 scene starts to those presets. The final player now samples shared pure HyperFrames timing presets for camera keyframes, staggered tool rows and partner orbit entry, the Word-card carry, and privacy loading dots. Motion IR remains the geometry authority and the scene-specific reference choreography remains source timed. The draft uses a finite paused GSAP timeline; the final player uses the same preset timing functions through its deterministic seek hook without a new runtime dependency. Browser verification swept all 20 kinds and confirmed the draft reproduces visible frames after out-of-order seeks. Five monochrome draft frames and 13 decoded MP4 frames were captured locally for review. The HyperFrames-integrated H.264/AAC export measured 1920×1080, 30 fps, 2,328 frames, 77.600 s, and 10,405,176 bytes.

After the owner's 20% assessment, a fresh read-only Product Designer audit ranked the remaining mismatches. The next cut replaces the uniform dotted galaxy with 1,697 deterministic source-sampled star centroids, a timed stream-to-spiral transform, and fixed glints; refits the 18.8/21 s composer crop, 60/71.5 s title metrics, and orbit tile scale; adds source-shaped dimensional tie/scales assets to the closing collage. At 8.5 s, sampled bright-pixel counts are closer to the reference, but that pixel check is only a local calibration and does not prove motion fidelity. A second independent Product Designer review found the composer, long title beats, and collage improved. It ranked the unsourced partner identity at 47-49 s as the largest mismatch. A final read-only check after the last light/camera edits found both 8.5 s and 21 s visibly improved, with smaller differences still present. The agent inspected still frames, not continuous motion; owner acceptance remains open.

For this recovery cut, controller verification passed: Motion IR schema validation, `node scripts/export-astra-law-video.mjs --verify` after the final visual edit, sampled `--proof` frames, matched reference/draft boards, `npm run build`, `npm run audit:all`, local SVG provenance checks, and `git diff --check`. An authored-style hash confirms out-of-order seeks restore the same DOM state; the browser capture gate permits at most 350 pixels with up to two levels of color-channel difference to account for observed Chromium raster variation at the 21 s macro text. Its path is `.cache/astra-for-law/astra-law-recreation.mp4`; reference audio and comparison assets remain ignored local files. The `audit:all` scripts primarily cover shared demos, so dedicated browser and frame checks carry this page's acceptance evidence. Source partner marks, exact Word interior, galaxy transition, and 74 s app collage still differ visibly; no numeric visual-fidelity score or owner approval is claimed.

On 2026-09-24, Astra supplied a review method: hash both MP4s, verify frame rate and duration, decode matching frame indices, scan coarse scene ranges and dense transition strips, and record observations separately from inferred causes and unknowns. A low-cost Codex Luna 6 batch used that method on the freshly exported MP4; its read-only report is in ignored `.cache/astra-for-law/diagnostics/astra-method-luna6-review.md`. Controller checked its findings against full-size source/output frames. The response-to-Word overlay at frame 1128 was real: shell text showed through the white handoff panel. The response lines now fade before the card crosses the gradient; a new H.264/AAC MP4 (SHA-256 `eefdb86616cc907cb719bc05070c320e1ec7d3de28bf15b74e376ba18cc74507`) decodes cleanly, and exact frame 1128 shows the intended blank white panel under the continuous card. The browser proof, player verification, 18 Motion IR/HyperFrames tests, TypeScript build, project audit, and whitespace check passed after the repair.

Two Luna findings were rejected after full-size frame inspection: at frame 1638 both videos already show “All bound by legal-grade,” so the claimed absent trust phrase is false; at frame 1272 the Word viewer's left edge and split are close to the source, so a broadly smaller window is not established. The 19.8–20.5 s composer crop remains visibly different (controls sit outside the recreated frame), and the unsourced partner marks at 47–49 s remain blank under the SVGL-only rule. Privacy character cadence and the vertical position of the legal-grade title still require matched-frame refinement. The post-repair MP4 has not received another full independent scene audit; visual acceptance and any brand-asset exception remain with the owner.

On the next owner review, the 14–18 s firm scene was judged too weak. Exact source frames at 14.0, 15.0, 15.4, 16.0, 16.6, 17.2, 17.8, and 18.2 s showed a left-anchored title that rises only after 15 s, a diagonal right-to-left row of 200 px tiles, changing tile color, a staggered left fade, and a 300 px Cooley focus with a pointer. `promo/astra-law-firm-motion.mjs` now drives those measured positions with continuous cubic motion; the final tile grows at layout size to keep its lettering crisp. This replaces the earlier single group shift and abrupt per-tile booleans. The owner also accepted arbitrary SVGL brands for unreadable partner marks; 19 local SVGs now replace the blank orbit slots. These are visual stand-ins, not source-identical legal partner logos. Matched browser frames were checked at the same scene times; owner review remains open.

The revised MP4 was exported with the local reference soundtrack after the last code edit and decoded without FFmpeg errors. It is H.264/AAC, 1920×1080, 30 fps, 2,328 frames, 77.600 s, and 11,137,664 bytes; SHA-256 `28f3f7076c5413074f1cff5ed596ab33654cb380d112c4704c37c05dfafbb7dd`. Frames 450, 480, 534, and 546 were decoded from this final file and visually compared with the same source indices. Browser verification including 16 s tile geometry, the 18.2 s Cooley handoff, pointer visibility, 19 SVGL stand-ins, repeated seeks, and reduced motion passed. All 18 Motion IR/HyperFrames tests, TypeScript build, `audit:all`, the standalone anti-flop gate, and `git diff --check` passed. The capture is materially closer in composition and motion, but user visual acceptance and broader 100% fidelity remain open.

## Pipeline contract hardening (2026-09-24)

The active worktree is `/Users/jang/.codex/worktrees/astra-law-recreation/design-os-svg-animation` on `codex/astra-law-recreation`; it contains the ongoing uncommitted Astra implementation and this documentation update. The [reference recreation contract](../docs/motion-video-recreation-pipeline.md) is now the normative entry point. It defines source intake, indexed state/transition evidence, mandatory HyperFrames v2 staging and sampled presets, Motion IR, deterministic seek/export, separate technical and visual gates, source-to-**encoded-MP4** comparison, acceptance states, and a cross-session handoff. The [workflow guide](../docs/motion-video-recreation-workflow-guide.md) holds the Astra lessons and low-cost reviewer method. `AGENTS.md`, the portable skill, README, site navigation/copy, and `docs/llms.txt` point to the contract rather than promising 99% parity or a conflicting five-stage process.

No promo scene or exporter code was changed in this documentation pass. The local private source remains `/tmp/astra-for-law-reference.mp4` (SHA-256 `5b22dc7d113480a1d28fc168dda081f3bd2680af71cab59a7d13985739db1888`). The latest encoded output remains `.cache/astra-for-law/astra-law-recreation.mp4` (SHA-256 `28f3f7076c5413074f1cff5ed596ab33654cb380d112c4704c37c05dfafbb7dd`), H.264/AAC, 1920×1080, 30 fps, 2,328 frames, 77.600 seconds. Its visual acceptance remains **open**; this documentation pass does not raise the artifact's acceptance state.

Documentation checks: relative Markdown links passed, stale 99%/five-stage claims were removed from the entry surfaces, `git diff --check` passed, standalone `python3 scripts/anti-flop-gate.py` passed, and `node --test tests/hyperframes.test.mjs` passed (2 subtests). Those gates do not establish visual fidelity. **Next action:** apply §6 of the contract to the existing encoded MP4, starting with a dense source/output transition strip around 14–18.2 s and continuous playback; log concrete gaps before changing animation code.

The owner subsequently requested a repository merge and GitHub marketing page update while visual acceptance remains open. The public page therefore labels Astra an **in-progress study** and does not publish the reference video/audio or local MP4. `scripts/sync-astra-law-pages.mjs` mirrors only the browser player's dependencies from `promo/` into the GitHub Pages source `docs/promo/`; `npm run verify:astra-pages` checks that public copy against the authored files. The landing page links to this browser player and the v2 contract, with a thumbnail extracted from the local recreation output.
